#include<stdio.h>
int main(){
    int x, y;
    for(y = 0; y<=15; y++){
        printf("\n numeros=%d", y);
    }

    
    //laço de repetição contando numeros pares
    for(x=0; x<=10; x+=2){
        printf("\n cont-numeros pares=%d", x);
    }
    return 0;
}